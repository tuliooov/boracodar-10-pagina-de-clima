import styled from 'styled-components'

export const Container = styled.header`
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.8rem;

  line-height: 1.9rem;

  img {
    width: 2.4rem;
    height: 2.4rem;
  }
`
